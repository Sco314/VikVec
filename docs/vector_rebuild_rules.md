# Vector Rebuild Rules

Do not pretend that a screenshot crop is a clean vector asset. A raster crop can be a useful starting point, but it is not the same as a true SVG.

- Rebuild tanks, pipes, rooms, and people with clean SVG shapes later.
- Group layers and assign meaningful IDs in the final SVGs.
- Avoid embedding raster images when creating true reusable vector assets.
