# Notes: segment-anything-annotator spike

- Purpose: track UI setup attempts, whether local mock SAM backends or static demos suffice, and whether export formats match VikVec manifest needs.
- Avoid installing heavy ML backends during initial evaluation.
